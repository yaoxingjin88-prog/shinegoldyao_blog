# 计算机设计大赛答辩问题准备

> 项目名称：ShineGoldYao-AI 驱动的全栈开发者博客与数字名片
> 作者：姚兴金
> 线上地址：https://shinegoldyao.store

---

## 一、项目概述类

### Q1：请用一分钟介绍你的项目

**参考回答：** ShineGoldYao是一个全栈技术博客系统，包含访客端（Nuxt 3 SSR）、管理后台（Vue 3 SPA）和 API 服务端（NestJS），共 24 张数据表、18 个后端模块、9 个前台页面、21 个后台页面。核心亮点是 **AI 深度融合**（智能对话、文章阅读辅助、写作副驾驶）、**Canvas 热力图 + 愤怒点击检测**、**WebSocket 实时通知**、**PWA 离线支持**、**知识图谱可视化**等。部署在 Vercel + Railway + Cloudflare，全站 TypeScript。

### Q2：你为什么要做这个项目？解决了什么问题？

**参考回答：**
- 市面上的博客系统（WordPress、Hexo、Hugo）要么缺乏 AI 能力，要么需要手动部署维护
- 我想打造一个**开箱即用、AI 原生**的个人技术博客，让写作和阅读都更智能
- 同时作为一个**全栈学习项目**，实践 NestJS + Nuxt 3 + Prisma 的现代全栈架构

### Q3：项目的技术选型依据是什么？

| 层级 | 选型 | 理由 |
|------|------|------|
| 前台 | Nuxt 3 SSR | SEO 友好、首屏性能好、支持 SSG/SWR |
| 后台 | Vue 3 + Element Plus | 管理后台不需要 SSR，SPA 开发效率高 |
| 服务端 | NestJS | 模块化架构、装饰器 DI、内置 Guard/Interceptor/Pipe |
| ORM | Prisma | 类型安全、Schema 即文档、迁移管理 |
| 数据库 | MySQL 8 | 成熟稳定、生态丰富 |
| 部署 | Vercel + 阿里云 | 前端全球 CDN、后端容器化、零运维 |

### Q4：项目有多少张数据库表？为什么这样设计？

**参考回答：** 共 24 张表，按业务域分为：

- **内容域：** article、article_category、article_tag、article_tag_relation（多对多）
- **用户交互域：** comment、message、social_link
- **个人展示域：** banner、skill、skill_category、experience、project
- **系统配置域：** site_config、admin_user、music、tool_category、tool_item
- **数据采集域：** visit_log、click_log、ai_chat_log、ai_read_log

所有表统一使用 `BigInt` 主键、`createTime/updateTime` 时间戳、`deleteTime` 软删除。

---

## 二、AI 相关（重点）

### Q5：你的项目集成了哪些 AI 功能？分别用在什么场景？

**参考回答：** 四大 AI 场景——

1. **AI 智能对话（航行者 AI）：** 前台右下角浮动聊天窗口，SSE 流式输出，支持技术问答
2. **AI 文章阅读助手：** 文章详情页侧边栏，一键生成 TL;DR 摘要、思维导图、术语解释；选中任意文本可触发 AI 解释
3. **AI 写作副驾驶（Co-pilot）：** 后台 Markdown 编辑器中选中文本，弹出浮层菜单，支持润色/重写/续写/精简/SEO TDK 生成，带 Diff 预览
4. **AI 文章元数据生成：** 一键生成文章摘要、SEO 关键词、推荐标签、SEO 描述

### Q6：AI 对话的防滥用机制是怎么设计的？

**参考回答：** 三层防护——

| 层级 | 机制 | 作用 |
|------|------|------|
| 前端 | localStorage 计数器 | 即时 UI 反馈，禁用输入框 |
| 服务端 | NestJS Throttler（20次/分钟） | 防止短时间高频请求 |
| 服务端 | IP 维度配额（ai_chat_log 表） | **核心防线**，按 IP 统计 24h 内使用次数，即使清 localStorage 或开无痕模式也无法绕过 |

**追问：为什么用 IP 而不用用户登录态？**
> 博客是开放访问的，不要求访客登录。IP 是匿名场景下最可行的标识。虽然同 Wi-Fi 下多用户共享 IP，但对个人博客场景够用。

### Q7：AI 流式输出（SSE）是怎么实现的？

**参考回答：**
1. 后端调用 LLM API 时设置 `stream: true`，拿到的是一个 ReadableStream
2. 后端逐块读取上游数据，设置 `Content-Type: text/event-stream`，直接透传给前端
3. 前端用 `fetch` + `ReadableStream` 逐行解析 `data: ` 前缀的 SSE 事件，实时追加到聊天窗口
4. 遇到 `data: [DONE]` 结束流

**追问：为什么不用 WebSocket 做 AI 对话？**
> SSE 是单向推送（服务端→客户端），正好符合 AI 回答场景。WebSocket 是双向的，开销更大，还需要额外的握手和心跳维护。SSE 更简单、更轻量、天然支持 HTTP/2 多路复用。

### Q8：AI 写作副驾驶是怎么检测用户选中文本的？

**参考回答：**
1. 监听 Markdown 编辑器（CodeMirror）的 `mouseup` 事件
2. 通过 `window.getSelection()` 获取选中文本和位置
3. 用 `Range.getBoundingClientRect()` 计算选区的屏幕坐标
4. 在该坐标位置渲染 `Teleport` 到 `<body>` 的浮层菜单
5. 用户选择操作后，调用 AI API，返回结果在侧边 Diff 预览面板展示

### Q9：AI 功能的后端架构是怎样的？

**参考回答：** 两个 Service 分工——
- `AiChatService`：负责前台对话，有 IP 配额限制，日志存 `ai_chat_log`
- `AiContentService`：负责阅读辅助、写作辅助、元数据生成，日志存 `ai_read_log`

两者共用 `.env` 中的 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL` 配置，通过 `ConfigService` 读取。模型默认 `deepseek-chat`，使用 OpenAI 兼容 API 格式。

---

## 三、热力图与行为分析

### Q10：你的热力图是怎么实现的？和市面方案有什么不同？

**参考回答：**
- **数据采集：** 前端监听 `click` 事件，记录归一化坐标（`offsetX/width`，`offsetY/height`，值域 0~1），通过 `sendBeacon` 批量上报，不阻塞页面卸载
- **数据存储：** `click_log` 表，字段包括归一化坐标、元素标签、页面路径
- **可视化渲染：** 管理后台用 Canvas 2D 手绘，每个点击点画一个径向渐变圆（从不透明到透明），通过 `globalCompositeOperation: 'lighter'` 叠加，最后用 LUT 色表（蓝→青→绿→黄→红）映射 alpha 值得到热力色彩
- **和市面方案的区别：** 没有使用第三方库（如 hotjar、clarity），完全手写；使用归一化坐标而非像素坐标，**跨分辨率兼容**——无论用户用 1080p 还是 4K 屏幕，数据都能正确叠加

### Q11：什么是愤怒点击（Rage Click）检测？算法是什么？

**参考回答：** 愤怒点击是用户体验问题的重要信号——用户在短时间内对同一区域反复点击，通常表示交互不符合预期。

算法：
- 维护一个滑动窗口（1.2 秒内）
- 检测窗口内是否有 >= 4 次点击，且点击位置在 40px 半径内
- 触发后记录一次愤怒点击事件，进入 10 秒冷却期避免重复记录
- 通过 WebSocket 实时通知管理员

---

## 四、实时通信

### Q12：WebSocket 在你的项目中用在哪些地方？

**参考回答：** 主要用在**管理后台的实时通知**——

1. 管理员登录后自动连接 Socket.IO 服务端，JWT 握手认证
2. 服务端推送 5 类事件：新访客、新点赞、新评论、新留言、愤怒点击
3. 前端收到通知后播放 880Hz 蜂鸣音 + `ElNotification` 弹窗
4. 支持指数退避重连（断线后 1s→2s→4s→...→30s 自动重连）
5. 通知中心下拉面板，最多缓存 50 条

**追问：为什么不用轮询？**
> 轮询延迟高（取决于轮询间隔）、浪费带宽（大部分请求返回空）。WebSocket 是全双工长连接，延迟 < 100ms，且只在有事件时才传输数据。

---

## 五、PWA 与性能优化

### Q13：PWA 的四层缓存策略是怎么设计的？

| 层级 | 缓存目标 | 策略 | 理由 |
|------|----------|------|------|
| 1 | API 请求 | NetworkFirst | 数据需要实时性，离线时回退缓存 |
| 2 | 文章页面 | StaleWhileRevalidate | 先展示缓存，后台更新，平衡速度与新鲜度 |
| 3 | 静态资源（JS/CSS） | CacheFirst（30天） | 带 hash 的文件名，内容变则 hash 变 |
| 4 | 图片 | CacheFirst（30天） | 图片很少变化，优先加载速度 |

**追问：PWA 安装弹窗的 7 天冷却期怎么实现的？**
> 拦截 `beforeinstallprompt` 事件，将事件引用存起来。用户关闭弹窗时记录 `localStorage` 时间戳，下次检查距上次是否 >= 7 天，不到则不弹出。

### Q14：你做了哪些性能优化？Lighthouse 分数多少？

**参考回答：** Lighthouse 综合 95+，LCP 0.9s。具体措施：
- **Nuxt SSR + 路由级 SWR 缓存：** 首页 60s、文章列表 60s、文章详情 300s、关于页 600s
- **代码分割：** lucide 图标、i18n locale 文件设为 manual chunks
- **图片优化：** `@nuxt/image` 自动转换 AVIF/WebP，响应式 srcset
- **字体/资源：** Gzip + Brotli 压缩（Nitro 内置）
- **事件上报：** `navigator.sendBeacon` 替代 `fetch`，不阻塞页面卸载
- **滚动监听：** passive listener
- **代码高亮：** 动态 import 语言包，用到才加载
- **Tree Shaking：** highlight.js 只注册实际用到的语言

---

## 六、工程化与安全

### Q15：全栈 TypeScript 带来了什么好处？

**参考回答：**
- **Prisma 类型链：** Schema → 生成类型 → Service 层自动推导 → Controller 层 DTO 校验，全链路类型安全
- **前后端共享接口类型：** 减少"前端传了但后端没处理"的接口不一致问题
- **编译时错误捕获：** 运行前就能发现属性拼写错误、类型不匹配等问题
- **IDE 智能提示：** 自动补全大幅提高开发效率

### Q16：你的项目有哪些安全措施？

| 类别 | 措施 |
|------|------|
| 认证 | JWT + bcrypt（10轮哈希），全局 JwtAuthGuard，`@Public()` 白名单放行 |
| 授权 | WebSocket 握手时校验 JWT，未认证直接断开 |
| 输入校验 | 全局 ValidationPipe + whitelist + forbidNonWhitelisted |
| 限流 | 三级 Throttler（1s/100、10s/500、1min/1000） |
| 内容安全 | 敏感词过滤模块（SensitiveModule） |
| 文件上传 | 仅允许 image/*，5MB 大小限制 |
| CORS | 生产环境严格白名单，开发环境自动放行 localhost |
| AI 防滥用 | 服务端 IP 配额，不依赖前端 |

### Q17：为什么要用软删除？怎么实现的？

**参考回答：**
- 所有业务表都有 `deleteTime` 字段（bigint，默认 0 表示未删除）
- 删除操作只是将 `deleteTime` 设为当前时间戳
- 查询时自动过滤 `deleteTime = 0` 的记录
- 好处：误删可恢复、保留审计记录、满足合规要求

**追问：硬删除什么时候用？**
> 目前没有硬删除。如果未来数据量过大，可以定期清理超过一定时间的软删除记录。

---

## 七、部署与运维

### Q18：你的部署架构是怎样的？

```
用户 → Cloudflare CDN (HTTPS) → Vercel (前端 SSR) / Railway (后端 API)
                                        ↕ Prisma
                                    MySQL 数据库
```

- **前端（web/）：** Vercel，Nitro 预设，全球 CDN 边缘节点
- **管理后台（admin/）：** Vercel，纯静态 SPA
- **后端（server/）：** Railway 容器化部署
- **HTTPS：** Cloudflare 自动 SSL 证书
- **域名：** shinegoldyao.store

### Q19：为什么不把前后端部署在一起？

**参考回答：**
- **独立扩缩容：** 前端是静态/SSR，后端是 API 服务，流量模式不同
- **独立部署：** 改前端不需要重启后端，改后端不需要重新构建前端
- **CDN 优化：** 前端可以享受 Vercel 全球 CDN，后端不需要
- **故障隔离：** 后端挂了前端还能展示缓存页面（SWR）

---

## 八、数据库设计

### Q20：article 表的 html 缓存字段是怎么回事？

**参考回答：** `article` 表同时存储 `content`（Markdown 原文）和 `htmlContent`（渲染后的 HTML）。发布文章时，后端将 Markdown 渲染为 HTML 并缓存到数据库。前端查询时直接返回 HTML，避免每次请求都实时渲染 Markdown，**减少服务端 CPU 开销**。

### Q21：visit_log 表记录了哪些信息？怎么解析浏览器和操作系统？

**参考回答：** 记录 IP、User-Agent、页面路径、浏览器、操作系统、设备类型。使用 `ua-parser-js` 库在服务端解析 User-Agent 字符串，提取浏览器名称/版本、操作系统名称/版本、设备类型（桌面/平板/手机）。

---

## 九、知识图谱

### Q22：知识图谱的力导向布局是怎么实现的？

**参考回答：** 纯 Canvas 2D 手写，没有用 ECharts 的图布局。物理模型：
- **库仑斥力：** 所有节点间互相排斥，防止重叠
- **胡克引力：** 有边相连的节点互相吸引，形成聚类
- **阻尼 + 降温：** 每次迭代速度衰减、温度降低，系统逐渐趋于稳定
- 节点大小由文章阅读量决定，颜色由分类决定

---

## 十、无障碍与国际化

### Q23：你的无障碍（A11y）支持做到了什么程度？

**参考回答：** 参考 WCAG 2.1 AA 标准，提供了 `A11yWidget` 组件：
- **字体缩放：** 用户可调整全局字体大小（16px/20px/24px）
- **高对比度模式：** 黑底白字，增强可读性
- **护眼模式：** 降低蓝光，柔和色调
- **减少动画：** 关闭所有 CSS 动画和过渡，照顾前庭功能障碍用户
- **链接高亮：** 给所有链接加下划线，方便色盲用户识别

### Q24：i18n 是怎么做的？支持几种语言？

**参考回答：** 支持中文和英文。使用 `@nuxtjs/i18n` 插件，策略是 `no_prefix`（URL 不带语言前缀）。语言检测优先级：Cookie 记录 > 浏览器 `Accept-Language` > 默认中文。Locale 文件懒加载，避免首屏加载不需要的语言包。

---

## 十一、开放性问题

### Q25：你在这个项目中遇到的最大技术挑战是什么？怎么解决的？

**参考建议（选一个最熟悉的讲）：**

**选项 A - SSE 流式透传：**
> NestJS 默认会缓冲响应，导致 AI 回答不是实时逐字显示的。解决方案是手动设置响应头 `X-Accel-Buffering: no`，调用 `res.flushHeaders()` 提前发送头部，并逐块 `res.write()` 透传上游数据。

**选项 B - BigInt 序列化：**
> MySQL 的 BigInt 主键在 JavaScript 中超过 `Number.MAX_SAFE_INTEGER`，`JSON.stringify` 会报错。通过猴子补丁 `BigInt.prototype.toJSON` 让它转为字符串解决。

**选项 C - 热力图跨分辨率：**
> 不同用户屏幕分辨率不同，如果用像素坐标存储点击位置，数据无法叠加。解决方案是存储归一化坐标（0~1），渲染时乘以当前画布尺寸。

### Q26：如果让你继续完善这个项目，你会加什么功能？

**参考回答：**
- **用户系统：** 支持访客注册登录，个性化推荐文章
- **评论嵌套回复：** 目前只有一级评论，可以加多级嵌套
- **文章版本历史：** 利用软删除记录，实现文章修改历史对比
- **AI 对话上下文记忆：** 当前每次对话独立，可以加会话管理
- **搜索引擎：** 接入 Elasticsearch，支持全文搜索和模糊匹配
- **Docker Compose 一键部署：** 简化自部署流程

### Q27：你的项目和 WordPress、Hexo 等成熟方案相比有什么优势？

**参考回答：**
- **AI 原生：** 这些平台的 AI 能力需要插件，我的是深度集成的
- **现代技术栈：** TypeScript 全栈、Nuxt 3 SSR、NestJS 模块化架构
- **数据自主：** 不依赖第三方平台，数据完全在自己的服务器
- **可定制性强：** 从 UI 到业务逻辑完全可控，不受限于插件生态
- **适合学习：** 本身就是最好的全栈学习项目

### Q28：项目的代码量大概有多少？

**参考回答：**
- 后端（server/）：18 个模块，约 3000+ 行 TypeScript
- 前台（web/）：9 个页面、22 个组件、4 个 composable、5 个插件，约 4000+ 行
- 管理后台（admin/）：21 个视图、多个组件和 Store，约 5000+ 行
- 数据库 Schema：24 张表，约 400 行 Prisma Schema
- 总计约 **15000+ 行** 业务代码

### Q29：你从这个项目中学到了什么？

**参考回答：**
- 全栈架构设计：如何拆分模块、设计 API、管理状态
- AI 工程化：不只是调 API，还要考虑防滥用、流式传输、错误处理
- 性能优化：SSR、缓存策略、代码分割、资源压缩的实战经验
- 工程化实践：TypeScript 类型安全、Prisma ORM、Swagger 文档、全局异常处理

### Q30：如果评委要求你现场演示一个功能，你最推荐演示哪个？

**推荐演示路径：**
1. 打开线上站点，展示首页和文章列表
2. 进入一篇文章，演示 **AI 阅读助手**（生成摘要 + 思维导图 + 术语解释）
3. 选中一段文字，演示 **AI 解释** 功能
4. 打开管理后台，演示 **AI 写作副驾驶**（润色/重写 + Diff 预览）
5. 展示 **热力图** 和 **愤怒点击检测** 的数据可视化
6. 展示 **WebSocket 实时通知**（另一端触发操作，后台实时弹窗）

---

## 附录：评委可能的追问清单

| 编号 | 追问 | 关键词 |
|------|------|--------|
| 1 | 为什么选 NestJS 而不是 Express/Koa？ | 架构选型 |
| 2 | 为什么选 Prisma 而不是 TypeORM/Sequelize？ | ORM 选型 |
| 3 | SSR 和 CSR 的区别？你哪些页面用了 SSR？ | 渲染模式 |
| 4 | 你的 AI 用的什么模型？为什么不自己训练？ | AI 技术 |
| 5 | SSE 和 WebSocket 的区别？为什么选 SSE？ | 实时通信 |
| 6 | 归一化坐标和像素坐标的区别？ | 热力图 |
| 7 | 软删除的优缺点？ | 数据库设计 |
| 8 | 你的 JWT Token 过期时间是多久？怎么刷新？ | 认证安全 |
| 9 | 前端状态管理用了什么方案？ | 状态管理 |
| 10 | 你的项目怎么保证代码质量？有测试吗？ | 工程化 |
| 11 | 数据库有没有做索引优化？ | 性能 |
| 12 | 你的 WebSocket 断线重连策略是什么？ | 可靠性 |
| 13 | 为什么前端不用 React？ | 技术选型 |
| 14 | Vercel 和 Railway 的免费额度够用吗？ | 部署成本 |
| 15 | 如果流量增大 10 倍，你的架构能撑住吗？ | 可扩展性 |
